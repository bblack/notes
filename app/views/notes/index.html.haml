%h1 notes
- if notes.length == 0
  %div.alert.alert-info
    You have no notes. Click the button to add one.
%div.container-fluid
  - rows.each do |row_notes|
    %div.row.notes
      - row_notes.each do |note|
        .col-md-3
          %a.note{href: note_path(note)}
            %h2= note.title
            %p.small.text-muted
              =time_ago_in_words(note.created_at)
              ago
  %div.row.notes
    .col-md-3
      %h2{style: 'text-align: center; opacity: 0.5;'}
        %a.note{href: new_note_path}
          .glyphicon.glyphicon-plus-sign
